<% if current_user == @course.instructor %>
	<div class="mdl-tabs mdl-js-tabs mdl-js-ripple-effect">
	  <div class="mdl-tabs__tab-bar">
	      <a href="#instructor-panel" class="mdl-tabs__tab is-active">Edit Course</a>
	      <a href="#student-panel" class="mdl-tabs__tab">Student View</a>
	  </div>

	  <div class="mdl-tabs__panel is-active content-width" id="instructor-panel">
	
		<%= render 'instructor_view' %>

	  </div>
	  <div class="mdl-tabs__panel content-width" id="student-panel">

	  </div>
	</div>
<% else %>
	Boo!
<% end %>