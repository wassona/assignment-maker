<script>
	let labelData = <%= raw @assignment.label_data.to_json %>
</script>


<%= form_for @result do |f| %>
	<%= @assignment.name %>
	<div id="image-wrapper" >
		<div id="auto-label-wrapper">
			<div id="new-div-wrapper"></div>
			<div id="assignment-divider-wrapper">
				<% 1000.times do |i| %>
					<div id="d<%= i %>" class="active-assignment-divider"></div>
				<% end %>
			</div>
			<img id="assignment-image" src="<%= @assignment.image.url %>">
		</div>
	</div>
	<button id="result-submit-button" >Submit!</button>
<% end %>
<div style="height: 200px"></div>